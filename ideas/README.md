# Idea incubator

Ideas are preserved even when unsuccessful. Each idea names its origin, model,
observable, new assumptions, a test that could refute it and the next bounded
calculation. Promotion to the claim ledger requires an explicit review; rejection
does not mean deletion. The initial register is `I001-action-field.md`.
