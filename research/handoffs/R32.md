# R32: single-calibration fibres and canonical pulse columns

2026-09-12. R32 complete for an explicit R30 pulse subfamily, C120–C121.
This stage began at clean HEAD 4287a8f, preserving its completed stage 1.

## Result

[The note](../../notes/single-calibration-fibres.md) constructs exact
common-ten-record curves when three supplied constraints are exact and one
has tolerance epsilon. Their leading tangents are d_j=L^{-1}e_j. Stage 2
chooses first pulse centres tau, 2 tau, 3 tau, for sufficiently small fixed
tau, followed by sufficiently narrow fixed positive widths. All four columns
have nonzero position and momentum components. Hence each single uncertain
constraint, including apparatus energy, gives optimal canonical product order
min(1,(epsilon/lambda)^2) with design/preparation-dependent action constants.

The key calculation is cubic Hermite interpolation at 1,2,3 and derivative
at 1. Initial value/derivative pairs are (-3/2,23/4), (3,-7), (-1/2,5/4),
(-3,11/2). A scaled ODE estimate and normalized finite-width inverse estimate
transfer these nonzero entries to actual smooth pulses. Analytic observability
selects a fourth disjoint signal pulse. The R30 apparatus box is chosen for
this explicit design and then stays fixed. No exact receiver energy is supplied.
The projected local curve has zero planar area.

This is an explicit admissible selection, not a theorem for every previously
allowed R30 design. For an arbitrary design the two relevant cofactors remain
the exact leading-order criterion. Zero leading components do not prove
canonical recovery.

## Audit and source capsule

Stage 1: one sequential Luna-low B63 audit of Freire pp. 1–3, with coordinator
source/proof review. Stage 2: one sequential Luna-low B64 worker, no descendants;
one discovery query and one primary DLMF §3.3 route. The coordinator waited,
reviewed the handoff, reopened the uniqueness/cardinal and confluence passages,
and added the requested explicit pulse-width operator estimate. Effective
worker settings were not independently verified. See [B64 review](../../reviews/pulse-column-B64.md).

Freire invariant-ball inverse -> exact coordinate-line pullback; inherited
Werschulz scalar information radius -> endpoint risks. DLMF interpolation and
confluence -> cubic cardinal columns -> early-pulse inverse response; normalized
width and inverse-entry margins decide transfer to the apparatus. These are
standard methods. Exact model matches and novelty remain unassessed.

## Next bounded step: R33

Retain this fixed design and box. In fixed output units choose a correlated
calibration direction r=L e_y, optionally normalized by a fixed positive
factor. Exact admissible curves T_lambda^{-1}(s r) have zero leading x and P
components. Compute the first nonzero coupling/nonlinear canonical response,
or prove exact constancy, before inferring recovery. Specify a scalar report
tolerance on this line and preserve three independent exact complementary
constraints. The uniform inverse still constructs the curve; the canonical
bound requires a new response test. Arbitrary-design zero cofactors remain
an additional open classification question.

## Validation

Written proof/source review complete. `make check` passed local Markdown
links, source companions, citation keys and source hashes. `make papers`
built all fourteen PDFs with no unresolved references or overfull boxes;
the new receiver section on page 20 was visually reviewed. Twelve unrelated
PDF rebuilds were restored after extracted text matched HEAD.
`git diff --check` passed. No numerical or symbolic verification scripts
were created or run; Python was used only for document integration and
PDF file/text integrity comparison.
