# R32: single-calibration fibres, first stage

2026-09-12. Stage 1 completed; R32 remains in progress. Entry HEAD 9299eee
already contained and pushed R31, so it was preserved without duplicate work.

## Result

[The note](../../notes/single-calibration-fibres.md) constructs an exact
common-ten-record receiver curve with three exact supplied constraints and
only the fourth allowed to change. Its parameter satisfies
abs(s)<=min(s_0,epsilon/lambda); its leading tangent is d_j=L^{-1}e_j.
C120 accepts this construction and the canonical risk bound conditional on
both canonical components of d_j being nonzero. The projected smooth curve
has zero planar area despite potentially positive coordinate risk product.

The domain is R30's fixed box and receiver energy ball; no exact receiver
energy, stochastic prior, or new pulse design is introduced. The reference
record is the exact coupled record through receiver zero. Only the constructed
local segment and coupling ceiling are reduced. All units are fixed.

## Audit and source capsule

One sequential worker, requested Luna low, no descendants: B63. The coordinator
waited, reviewed its outputs and reopened Freire pp. 1–3. Zero discovery queries;
one source route, retrieved separately by worker and coordinator. Effective
worker settings were not independently verified. Source methods are standard;
exact model matching and novelty remain unassessed.

Freire's invariant-ball inverse construction -> fixed image interval -> exact
coordinate-line pullback. Werschulz's inherited scalar information-radius idea
-> canonical endpoint risks rather than a dimension count. Coordinator narrowed
the worker's centre-estimator wording to the compact scalar case and clarified
that Freire's full inverse differentiability proof continues beyond p. 3.

## Next bounded step

Evaluate d_4: A_1(d_4)=A_2(d_4)=A_3(d_4)=0 and -Kc B_1(d_4)=1 in
the fixed output units. Determine whether R30's existing pulse-selection
conditions force both x and P components nonzero. If they do not, state an
additional admissible pulse-selection condition and prove its feasibility,
keeping that design change explicit. Then compare j=1,2,3. A zero leading
component calls for an exact/higher-order test, not a recovery assertion.

## Validation

Written proof review: [B63](../../reviews/single-calibration-B63.md).
`make check` passed 1315 local Markdown links, source companions, citation keys
and source hashes. `make papers` built all fourteen PDFs; build output reported
no unresolved references or overfull boxes. The new receiver result on p. 20
was visually reviewed. Twelve unrelated PDF rebuilds were restored after their
extracted text matched HEAD. `git diff --check` passed. No numerical or symbolic
verification scripts were created or run.
