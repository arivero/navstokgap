# R25: full apparatus preparation and whole-shell ambiguity

Completed derivation and review, 2026-09-12:
[proof](../../notes/full-apparatus-preparation-ambiguity.md),
[review](../../reviews/full-apparatus-B56.md), C106–C107 and receiver section 30.

All ten unscaled final apparatus records have the invertible free apparatus
shear as their zero-coupling limit. Preconditioning by its inverse gives
D_eta F=I+O(lambda), D_z F=O(lambda), uniformly on a compact finite-time domain.
A self-mapping contraction constructs an exact compensator with displacement
at most 2 L lambda times receiver displacement, retaining a b/2 interior margin.
R17's shell chart then supplies a local canonical square and positive risks.

The stronger fixed-width consequence uses the compact shell diameter from one
nominal point: lambda<=min(lambda_0,b/(4 L D)) permits every shell point to
share one complete final record. Completing the energy square gives canonical
ellipse k_x x^2+P^2/mu<=2E, k_x=a-g^2/d. Common-record endpoints and the
constant zero estimator prove exact minimax radii sqrt(2E/k_x), sqrt(2mu E),
optimal risk product 2E sqrt(mu/k_x), and projected area pi times that product.
The scale is receiver-energy-dependent; the regime depends on box width.

Preparation is full Cartesian shell times apparatus box. Exact initial
apparatus/total energy is not supplied. Free probe drift, clock reaction,
known T, fixed coordinate units and common cutoff margins are retained.
This is deterministic reconstruction risk, with no probability-density,
accuracy–disturbance or universal positive-action conclusion.

B56 used one sequential gpt-5.6-luna worker at requested low effort and no
descendants. The worker read two local companions, B48 and B55, with zero
queries, zero retrievals and no new source-page reading. Coordinator reviewed
both artifacts, clarified unverified chapter authorship and inherited-reading
wording, and checked every new estimate in writing. Source capsule: inherited
Banach contraction plus compact smooth-flow dependence -> precondition the
free apparatus shear -> construct exact receiver compensation -> the full
preparation product and b/lambda ratio decide the result. Novelty unassessed.
No numerical or symbolic verification scripts ran.

Next R26: impose exact initial apparatus kinetic energy H_app(eta)=
H_app(eta_*), with all initial pulse couplings zero. Restrict this scalar to
R25's unique compensator eta(w) on the receiver energy shell. Establish an
actual common-record family with canonical separations and preparation margins,
or a recovery estimate; do not infer either outcome from dimension alone.
The full-shell result of R25 does not carry this extra constraint.

Validation: `make check` passed 1177 local Markdown links, source companions,
citations and source hashes. `make papers` built all fourteen PDFs. The receiver
has sixteen pages; its new section was visually checked, and receiver/programme
logs have no warnings, undefined references or overfull boxes. Unchanged-paper
PDF rebuild churn was removed after equal extracted-text checks. Final
`git diff --check` passed. Coordinator checkpoint follows this handoff.
