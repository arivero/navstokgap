# Restart skills checkpoint — 2026-09-08

The research skill now selects among the five positive-action obligations.
A second skill reloads task-specific bibliography ideas after context loss,
using local companions and a concrete source-to-test capsule. AGENTS provides
the entry hook; ordinary context recovery adds no searches or worker calls.
The skill-creator guidance shaped the short entrypoints and progressive routing.

## Artifacts and verification

- Updated `skills/principia-action/SKILL.md` preserves historical and geometry
  guidance while adding receiver, universality, cut, spectral and coherent routes.
- Added `skills/navstokgap-bibliography/SKILL.md` and
  `references/IDEA_BRIDGES.md`, with five task-routed source connections.
- Updated AGENTS, STATE and TASKS for fresh-session and compaction recovery.
- Both skill validators passed. `scripts/check_repository.py` passed with 561
  local Markdown links, source companions and citation keys; `git diff --check`
  passed. Routing reviewed against G02, A09, historical worker and retained-capsule
  cases. No independent agent test was commissioned for this bounded tooling edit.
- Local discovery: the existing Principia skill symlink points to the updated
  repo skill. Added the analogous `navstokgap-bibliography` symlink under
  `/home/codexssh/.codex/skills/`. Other clones use the tracked AGENTS hook;
  this local discovery link is optional and outside version control.

## Resume G02 from the saved worker, rather than dispatching again

B22 was requested as GPT-5.6 Luna, low, sequential, no descendants. Its source
companion and handoff are saved; source/proof acceptance remains pending. The
worker's provisional C045–C046 labels have not been entered into the ledger.
Recheck live IDs before assigning them.

Coordinator review flags: corrected the handoff's count to four pages
(169–170, 285–286). Check the second-edition title-page author/contributor
credits and the Chapter 20 normalization. The intro describes a lifted sum;
the body specifies a uniform average. Text extraction alone leaves equation
image verification pending. Keep the cached PDF outside tracked sources until
redistribution rights are established.

The prepared four-state test uses independent signs with flip rates
$\lambda,\epsilon$. The observables $us,u\delta r,u\delta sr$ have frame
minimum $u^2\delta^2$ and summed susceptibility
$u^2[1/(2\lambda)+\delta^2/(2\epsilon)+\delta^2/(2(\lambda+\epsilon))]$.
Set $\epsilon=\lambda\delta^2$ to test bounded response as coverage vanishes.
The injective velocity $v_\delta=u(s+\delta r)/\sqrt2$, $0<\delta\le1/4$,
has fixed plateau $mu^2/\lambda$ while the gap $2\lambda\delta^2$ closes.
Reconstructing $ur$ from velocity needs Lipschitz gain at least $\sqrt2/\delta$.
These are draft calculations for integration and checks, not accepted claims.

For independent factors, use the centered tensor-sector decomposition:
local observables miss the mixed sector, whose eigenvalues are sums. Factor
frame bounds can therefore control the full product gap through independence.
Track fixed per-factor clocks against a fixed total clock. Extend the existing
susceptibility note/PDF and add exact finite tests before promoting results.

Next physical question: which conservative receiver supplies both calibrated
observable access and preparation-independent correlation scale? Use the first
source-idea card to keep this connected to the action-selection target.
