# R19: full pointer records recover a fixed preparation box

Completed 2026-09-11: [proof](../../notes/full-pointer-recovery.md),
[review](../../reviews/full-pointer-B50.md), C094–C095 and receiver section 24.

The scaled eight-record map is uniformly C1-close to (-A_c z,q) on a convex
receiver neighbourhood times a fixed positive position box. Known incoming
clock data range over a small compact box with uniformly invertible signal
matrices; incoming probe momenta are also known. A segment bound gives global
injectivity for sufficiently small fixed positive coupling. Minimum-residual
fitting bounds the canonical product by 16 L_*P_* max(rho_pi/lambda,rho_q)^2
/ beta^2. Improving final records closes it without contracting preparation.

The coordinator derived and reviewed the finite-time variational estimates,
clock reaction, convex-domain inverse, estimator and units. One sequential
worker requested as gpt-5.6-luna, low effort, performed B50 within one query,
one primary source and four selected PDF pages. Effective model/effort was
not independently reported. Coordinator verified Sideris metadata and selected
text and visually checked printed p. 92. No numerical/symbolic scripts ran.
Source and model result status are separate; novelty remains unassessed.

Source capsule: Sideris chapter 6 (printed pp. 89–90, 92–93) -> smooth flow and
variational equations -> uniform record-map perturbation -> common compact
interval, cutoff margins and fixed signal inverse decide recovery. B49 supplies
the local/global observation distinction. Exact incoming clock/momentum and
joint final access at known T remain supplied resources.

Next R20: retain eight final pointer records and known incoming probe momenta,
but hide initial clock position and momentum. The R19 inverse suggests solving
for receiver and probe positions as clock data vary; impose the exact receiver
energy shell and interior preparation margins before claiming an ambiguous
family. Dimension counting alone does not decide canonical ambiguity.

Validation: `make check` passed 1056 local Markdown links, source companions,
citation keys and source hashes. `make papers` built all fourteen PDFs; the
receiver is twelve pages, with section 24 visually checked on page 12. The
receiver/programme logs contain no warning, undefined-reference or overfull-box
entries. `git diff --check` passed. Unchanged-content PDF churn was excluded
after comparison of extracted text against HEAD.
