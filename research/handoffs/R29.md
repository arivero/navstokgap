# R29: local recovery at two globally ambiguous branches

Completed 2026-09-12: [proof](../../notes/two-calibration-branches.md),
[review](../../reviews/two-calibration-B60.md), C114–C115 and receiver section 34.
R27/R28 were already committed at entry (HEAD b3efb80), so neither was repeated.

Add q_2=0 to R28. Its divided exact compensator residual adds A_2=integral
t f_2 x. Evaluation rows x(tau), xdot(tau), x(2tau) have a kernel generated
by Phi_-tau(0,0,Y,Q), with Y=-Q tau/(3nu)+O(tau cubed). Its initial x and P
have leading coefficients -gQ tau cubed/(3mu nu) and 5gQ tau squared/(6nu).
Fixed positive widths preserve rank and canonical components. Choose the
nominal receiver point on this kernel line and the energy shell.

Both opposite shell points solve the zero-coupling scaled residuals, with
invertible four-row derivatives. The positive point stays exact; the negative
point continues with O(lambda) correction. Both exact preparations satisfy
q_1=c, q_2=0, both energies and one full final record, with b/2 margins.
The global canonical risk lower bound tends to abs(z_bar_x z_bar_P)>0.

At each fixed positive coupling the augmented fourteen-input/output map is
locally invertible at each branch: eliminate the apparatus-record block,
then use the four residual rows, three multiplied by lambda. Local convex
neighbourhoods give stable inverse bounds; no uniform unscaled lambda-to-zero
margin is asserted. The pair is not the original calibrated sign involution.

B60 used one sequential gpt-5.6-luna worker requested low effort, no
descendants, zero queries/retrievals and inherited B59 passage coverage only.
Effective settings were not independently reported. Coordinator reviewed the
new coefficients, rank, quotient smoothness, branches and local/global scope
in writing. Exact prior-art matches and novelty unassessed. No mathematical
verification scripts ran.

Next R30: add q_3=0, selecting A_3 nonzero on the remaining kernel line.
Four independent leading linear constraints may give a global inverse and
make receiver energy redundant. Control exact constraints uniformly as both
receiver and final record vary; disappearance of the selected two roots is
not global recovery. Keep preparation boxes and division-by-lambda noise
conditioning explicit.

Validation: `make check` passed 1254 local Markdown links, citation keys,
companions and source hashes. `make papers` built all fourteen PDFs. The
receiver has eighteen pages; section 34 was visually checked on its final
page. Receiver/programme logs contain no warning, undefined-reference or
overfull-box entries. `git diff --check` passed. Unchanged PDF rebuild churn
was excluded from the checkpoint.
