# A10 conservative receiver — 2026-09-08, checkpoint 2026-09-09

C047 derives the tagged displacement-action observable from a connected finite
spring network and an invariant fixed-energy phase ensemble. Fixed centre
velocity yields a zero long-window limit; independent random centre velocity
adds a ballistic term. The exact two-body receiver, speed margin, energy
scaling and necessary large-receiver bound are part of the same result.

Artifacts: `notes/conservative-harmonic-receiver.md`, generated LaTeX/PDF,
`scripts/harmonic_receiver_checks.py`, its JSON output, B23 companion and review.
Eight identities, three explicit network checks and four bound cases pass.
The paper build produced 15 PDFs, including the two-page receiver note;
its final page was visually checked. `make check` passed, including all source
hashes; the repository checker passed with 588 local Markdown links.

One sequential librarian was requested as GPT-5.6 Luna, low. The worker used
two searches and reported nine source pages against a six-page budget; the
coordinator corrected the count and commissioned no further worker. Coordinator
checked Ford–Kac–Mazur p. 505 and Zwanzig p. 219 visually, reviewed normalization,
and recorded PDF hashes. Sources stay cached pending redistribution rights.

Source-to-task capsule: harmonic propagation gives finite cosine velocity
correlations; oscillator-bath elimination gives a friction memory kernel.
These are different observables linked by the reduced dynamics. A11 must choose
a concrete growing spring network and energy allocation before identifying the
tagged low-frequency measure or any surviving action scale. Finite total energy
also supplies a frame-dependent speed margin that must be tracked with size.

Next task A11: compute both iterated receiver-size/window limits and a useful
joint regime. Track centre momentum, low-frequency weights, preparation energy,
and any loss of uniform displacement bounds. Then commission one bounded
prior-art audit before acceptance. A09 retains universal preparation independence
and quantum-role selection; no positive universal action parameter is selected
by C047's finite model.
