# H06: pre-1901 source batch

- **Task:** H06, locate and transcribe pre-1901 primary sources useful as
  retrieval context for later agents, as instructed by the user on 2026-09-08.
- **Role/model/effort:** interactive Claude Fable 5.1 session; no subagents,
  no ultra effort; web search for routes and `curl` for retrieval.
- **Date:** 2026-09-08.
- **Outputs:** `docs/classics/` with twelve sources, companions and an index;
  `docs/SHA256SUMS` entries for every stored original; twelve BibTeX entries;
  `references/batches/H06.md`; a `docs/README.md` link; one TASKS row and one
  STATE pointer.

## Coverage

Routes, reading levels, OCR line ranges, access failures and exclusions are in
the batch note. Wilkins' transcriptions are stored as PDF and TeX; Wikisource
texts as rendered HTML with a pandoc transcription in the companion; archive.org
books as OCR excerpts with archive line ranges; Perseus as a TEI chunk and
plain Greek. Jacobi's excerpt spans archive lines 2685–3421.

## Checks

`make check` verifies companions for every PDF, HTML and XML in `docs/`,
registered checksums, local links and unique BibTeX keys. The Archimedes
four-thirds identity was checked with SymPy. OCR formulae remain targets for
verification against the scans.

## Next bounded task

Visual verification of the Euler, Archimedes and Jacobi formulae; then the
H02 use of Gregory's preface and the H05 use of the Plutarch text. A second
batch is listed in the batch note.
