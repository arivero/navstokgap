# R23: final clock momentum restores global recovery

Completed 2026-09-11: [proof](../../notes/final-clock-momentum-recovery.md),
[review](../../reviews/final-clock-B54.md), C102–C103 and receiver section 28.

The nine-record map is L+R, L=(-A_v z,q,v), with C1 remainder O(lambda)
including the complete clock reaction. In fixed units, last blocks bound
speed and position differences. The receiver block then gives a global
lower Lipschitz constant beta=min(1,alpha/(1+D)), D=sup||(partial_v A_v)z||.
Segment integration bounds only R on the convex initial-data domain, giving
the exact map margin beta/2. No convex-image argument is needed.

Global recovery holds on a bounded receiver ball, so exact energy and local
patch knowledge are unnecessary. With delta=max(rho_pi/lambda,rho_q,rho_c),
the canonical product is bounded by 16 L_*P_* delta squared/beta squared.
Preparation widths, masses and coupling remain fixed as record errors close.
Known offset, zero incoming probe momenta, exact calibration and joint final
record access remain supplied. The final clock momentum is persistent; its
reaction correction is calibrated jointly, not treated as zero.

B54 used one sequential worker requested gpt-5.6-luna at low effort, zero
discovery queries, one existing Freire source, PDF pp. 1–3. Effective settings
were not independently reported. Coordinator re-read these pages and visually
checked Proposition 1 on p. 3. Standard perturbation precedent and the model's
nonlinear-reference estimate are kept separate; novelty unassessed.

Next R24: hide initial offset, add final clock position at known T. The leading
full clock block (s+vT,v) is explicitly invertible. Bound A_(s,v) variation
to obtain a full ten-record global inverse on fixed convex boxes, then
consolidate R19–R24 by supplied preparation data and final record access.
Physical readout of those records by another apparatus remains a separate
measurement-composition task; do not silently assume it has been implemented.

Validation: `make check` passed 1136 local Markdown links, source companions,
citation keys and source hashes. `make papers` built all fourteen PDFs. The
receiver remains fourteen pages; section 28 was visually checked on its last
page. Receiver/programme logs contain no warning, undefined-reference or
overfull-box entries. `git diff --check` passed. Unchanged-content PDF
timestamp churn was excluded. No numerical/symbolic verification scripts ran.
