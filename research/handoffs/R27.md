# R27: calibration leaves a full-state topological ambiguity

Completed 2026-09-12: [proof](../../notes/calibrated-displacement-ambiguity.md),
[review](../../reviews/calibrated-displacement-B58.md), C110–C111 and receiver
section 32. Live R26/B57 state superseded the older R24 restart memory.

The known q_1=c is nonzero with |c|/L_1<b/4. Both initial energies remain
exactly E,H_0 and all ten final apparatus coordinates are supplied at known T.
Solving apparatus energy for positive clock momentum leaves eight free
apparatus coordinates. Solving receiver energy for upper-branch y leaves
x,P,Q free. A fixed radius-r ball in this eleven-coordinate chart has strict
radicands and b/2 preparation margins, independently of weak coupling.

The boundary is S^10; Borsuk–Ulam gives parameter antipodes with identical
complete final records. This is an exact existence result, not an explicit
physical sign pair or an infinitesimal rank count. R25's inverse-shear
estimate gives apparatus separation <=2L lambda times receiver separation.
The chart parameters are a projection of the initial coordinates, hence
receiver separation >=2r/sqrt(1+4L squared lambda squared). Half this distance
bounds deterministic full-state minimax error in fixed dimensionless component
units. It does not force differences in both x,P or yield an action product.

Source capsule: Arora's Princeton Lecture 13 (2002 header, 2005 archive),
Theorem 1, PDF p. 1 -> continuous S^n to R^n has equal-image antipodes -> apply
to the constrained preparation sphere -> preparation dimension/margins and
the separate receiver-separation estimate decide what is observable.

B58 used one sequential gpt-5.6-luna worker requested low effort, no descendants,
one query and two source opens. Effective settings were not independently
reported. Coordinator independently checked the primary theorem and reviewed
all model equations in writing. Imported theorem established; mechanical
application derived; novelty unassessed. No mathematical verification scripts ran.

Next R28: test actual equal-record separation in both x and P with the same
nonzero calibration and exact energies. A full-state difference could lie in
y,Q. Seek a canonical pair with quantitative margins or a canonical recovery
result; a norm bound or a count of free parameters alone will not settle it.

Validation: `make check` passed 1215 local Markdown links, companions,
citation keys and source hashes. `make papers` built all fourteen PDFs.
Receiver section 32 was visually checked on pages 16–17; receiver and
programme logs have no warning, undefined-reference or overfull-box entries.
`git diff --check` passed. Twelve unchanged-content PDFs were restored after
text comparison. Only document/integrity tooling used Python.
