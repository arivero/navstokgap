# R15 handoff: independent apparatus composition

R15/B46 complete, 2026-09-11. C086–C087 settle the apparatus-regrouping test:
at r=3/2, k independently budgeted identical blocks have k times the saturated
one-block canonical risk product. A single fixed global budget has an invariant
product. Matching its centre risks with separate blocks requires allowances
shrinking as k^(-2/3), while the full error sets still differ.

## Work and provenance

Coordinator derived and reviewed [the proof](../../notes/block-apparatus-composition.md),
integrated receiver section 20, the claim ledger and restart state. Initial
worktree was clean. No numerical or symbolic verification scripts were used;
Python was used only for document editing/build and repository integrity.

One sequential worker requested gpt-5.6-luna, low effort, no descendants;
effective settings were not independently reported. Coordinator waited for
completion, then verified both primary passages and reviewed the written proof.
[B46](../../references/batches/B46.md) and its companion record two queries,
two selected readings, corrected exponent/anchor, and exact limits of coverage.
The worker handoff remains unchanged for provenance.

## Result and assumptions

All constituents have fixed positive m,F, known initial phases and arbitrary
bounded force histories. Each independent block has a pointwise l^r error
budget. Complete position histories are supplied without apparatus back-reaction.
The note gives all finite-horizon block risks and exact whole-record image;
unequal block precision loses momentum information under aggregation after
saturation. H is a product of scalar minimax coordinate risks with action
units, not a joint trajectory error bound or selected quantum constant.

Product constraints regroup associatively when kept intact. Replacing them by
one global norm ball changes preparations; equal centre risks can hide that
change. Tolerances specify an error class, not an energetic apparatus cost.

## Restart capsule — R16

R14/B45 norm geometry plus R12 product support addition produces R15's exact
resource comparison. B46 optimal-recovery definitions support declaring the
uncertainty set before composing it. R16 should now use R06's finite mechanical
records: identify the finite record-error vector and preparation class, hold
mass, coupling and duration fixed, and test uniform contraction of that error
class. Trace any obstruction to an explicit physical premise. Keep the finite
records separate from R15's complete-history oracle and preserve preparation
and quantum-phase selection as distinct gates. Read R06's handoff and
notes/autonomous-finite-readout.md; B36's thermal passages supply a comparison.

## Validation

Written review covers projection/lifts, canonical normalization, finite-horizon
transfer, Jensen equality, saturation timing, enclosing-ball minimality,
strict inclusions, norm endpoints and precision contraction. Document/build
validation is recorded below after completion.

`make check` passed 988 local Markdown links, source companions, citation keys
and all recorded source hashes. `make papers` built all 14 PDFs. Corrected
new-section math typesetting after the first build; the final receiver and
programme logs contain no warnings or box defects. Visually inspected receiver
section 20 on page 10. Restored this run's regeneration of 12 unaffected PDFs;
retained the receiver and programme editions. `git diff --check` passed.
