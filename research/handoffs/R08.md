# R08: exact reachable cuts and observed-position area

Completed 2026-09-10 from 5e397ad. C072 proves exact cut composition and a
strict rectangle counterexample. C073 gives the exact observed-position
momentum fibre and terminal area, including area closure without momentum
recovery. See [the proof](../../notes/reachable-cut-composition.md) and
[coordinator review](../../reviews/cut-composition-B39.md).

The same measurable bounded-force class, mass and total horizon are retained.
An unobserved cut supplies no record; observed cuts are ideal non-disturbing
information available for terminal prediction. No observation apparatus is
claimed. The full-state area is $2F^2b^3/(3m)$; position-only area adds
$Fb^2D/m$ for the explicitly derived compatible momentum width $D$.

## Work and source capsule

Read live state, programme, R08 task, both repository research skills, the
R07 handoff and B38 capsule; preserved a clean initial worktree. One sequential
Luna worker at requested low effort completed B39 within two searches and a
three-passage cap. Effective settings were not independently reported.
Coordinator waited for completion, verified the primary passages and reviewed
all formulas in writing. No descendants or numerical/symbolic scripts.

Source → construction → next test: Bird et al., arXiv:2106.14831v3 abstract
and §4.1 Theorem 8 give exact set propagation in a discrete hybrid setting.
Borrow compatible-state propagation for R09: intersect the lens with a
position strip of half-width $\varepsilon$, then shear and add the future lens.
Keep correlations; rectangular outer approximations require separate labeling.
Liberzon §4.4.1 retains the bounded double-integrator precedent. Neither source
states our conditional canonical-area formula; novelty remains unassessed.

Outputs: R08 note, C072–C073, B39 batch/companion/worker handoff, coordinator
review, receiver manuscript summary, research state/programme/targets/task board
and source-idea capsule. R07's note now links its completed cut comparison.

## Validation and next task

`make check` passed: 843 local Markdown links, source companions, citation keys
and all registered source hashes. `make papers` rebuilt all 14 PDFs without
unresolved references or layout overflow. `git diff --check` passed. All
mathematical validation is written proof review; Python was used only for
document editing and repository document/build checks.

R09: finite-precision position records at fixed horizon. Derive the exact
compatible terminal set and area in the joint precision/residual-delay limit;
identify the premise that could obstruct closure. A19 remains supporting work.
