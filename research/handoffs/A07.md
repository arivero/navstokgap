# A07: sharp bounded-acceleration return

Completed 2026-09-08. C043 proves feasibility iff $T\ge2u/a$ and unique
minimum kinetic cost $mu^3/(3a)$. C044 proves the sharp polygon error
$S_K-S_\pi\le ma^2\sum d_i^3/24\le ma^2T|\pi|^2/24$.

Maintained note: `notes/bounded-acceleration-return.md`; generated LaTeX/PDF.
Checks: `scripts/bounded_acceleration_checks.py`, 13 symbolic identities,
two excess-cost checks and 12 rational partition cases. B21 is one sequential
gpt-5.6-luna low librarian audit, followed by coordinator proof/source review.
No descendants or parallel delegation. Effective worker setting unverified.

The source search gives a bounded-control framework match; the exact kinetic
cost is an elementary derived specialization, with no novelty claim. Coordinator
corrected the worker's terminal-envelope sign. The Principia/action skill kept
the positive endpoint cost distinct from vanishing polygon error and arbitrarily
small excess costs. Source guidance records exact reading depth.

## Restart

Verification complete: `make check` passes and `make papers` builds fourteen
accepted PDFs with resolved references and no layout overflow. The new
three-page PDF's minimum-cost theorem page was visually checked.

Continue **G02**: weaken access to the four-state model's internal label,
tracking observable coverage and total susceptibility under composition.
The next mechanical realization must specify a conservative force/receiver
instead of choosing an arbitrary bounded external control. A09 retains the
preparation-independent action scale and coherent-composition selection target.
M03 and I003 retain their own review gates.

For interpreting A07: at fixed $F_{\max}$ the cost is $m^2u^3/(3F_{\max})$;
lowering endpoint speed closes it. At fixed acceleration, partition refinement
uniformly recovers the kinetic action. A positive approximation remainder needs
loss of uniform acceleration control in the tested fixed-duration setting.
