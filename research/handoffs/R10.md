# R10: two-record recovery and its prior-art match

Completed 2026-09-10 from clean 6616a75. Read live state/tasks/programme,
the action and bibliography skills, retained cut-route context and B40 capsule.
C076 gives exact compatible-set propagation and sharp errors for the specified
finite-difference estimator. C077 gives recovery of both coordinates and a
positive-delay family with vanishing action-error product at fixed $m,F,T$.
These are upper bounds for the best possible estimator, not a minimax theorem.

One sequential worker requested gpt-5.6-luna, low effort, completed B41 using
two queries and three primary passage selections. Effective settings were not
independently reported. Coordinator waited, then reviewed source text and proof.
Additional §4 reading found the exact prior-art constants, upgrading the worker's
scaling match. Corrected $v''$ to $q''$ in its companion and versioned the HTML
link; the source is arXiv v2, revised 2 August 2022. A guessed v3 route returned
404 before the abstract's actual HTML link was followed. No source originals
were downloaded, no descendants or numerical/symbolic scripts were used.

## Result checks and source-to-task capsule

Written checks cover the weighted force integral, adverse noise signs,
simultaneous saturation with fixed initial state, rectangle area factor four,
timing feasibility, and the positive-delay constants $R_q=7\varepsilon$ and
$R_p=4\sqrt{mF\varepsilon}$. Full proof and review are in the linked task files.

Seeber–Haimovich §4 supplies the established finite-difference bound and optimum;
§6.2 suggests indistinguishable parabolic arcs. For R11 borrow the latter
construction but enforce shared initial state and exactly the available records.
Proposition 6.5's quasi-exactness premise belongs to a different estimator class.

## Next task: R11

Build a same-record pair at fixed precision and separation, with both forces
bounded by $F$ and the same $(q_0,p_0)$. Half their terminal momentum separation
is a lower bound for every estimator receiving those records. Compare it to
R10's upper bound and state the attainable parameter regime. Only then test an
apparatus restriction that might keep an action-valued scale positive as
precision improves. Momentum reconstruction and canonical information area are
different observables; retain both explicitly.

## Validation

`make check` passed 876 local Markdown links, source companions, citation keys
and registered hashes. `make papers` built all 14 PDFs. Restored only this run's
incidental rebuild changes to the 12 unaffected PDFs. Shortened the receiver
summary to remove a two-line spill; rebuilt it with three LaTeX passes and
visually inspected final page 7. The updated reading editions are receiver and
programme. `git diff --check` passed. Python ran only permitted document checks
and builds; all mathematics was reviewed in writing.
