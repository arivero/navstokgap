# G01: spectral control by an observable collection

Completed 2026-09-08. C041 gives the susceptibility/gap product and a fixed
positive velocity plateau with a closing hidden-label gap. C042 gives the
lower bound $\gamma_1\ge\alpha/\sum_a\chi(f_a)$ when the centered
observables have frame lower bound $\alpha>0$.

## Outputs and audit

- `notes/susceptibility-gap.md`, generated LaTeX and accepted PDF.
- `scripts/susceptibility_gap_checks.py`, `out/susceptibility-gap-checks.json`:
  11 identities, five rational bound cases and four weighted-chain checks.
- B20 companion, batch and review. One sequential gpt-5.6-luna low librarian;
  no descendants or parallel work. Effective setting independently unverified.
- Cached Pavliotis pp. 4–5 read, p. 5 visually verified by coordinator;
  Sokal remains a discovery-only route. The exact frame presentation is an
  elementary derived consequence, with novelty unassessed.

The Principia/action skill kept action units, inverse-time gaps and supplied
energy conversion distinct. Source guidance retained a precise source match
for Green–Kubo and separate status for the local spectral arguments.

## Restart and next physical work

Verification: `make check` passed (including the concurrent session's then-current
I003 checks), with 533 local Markdown links and all source checksums valid.
`make papers` built thirteen accepted PDFs without unresolved references or
layout overflow; the three-page G01 PDF was visually checked on its theorem page.

**A07 is the next bounded mechanics task:** replace instantaneous flips by
bounded-acceleration returns with prescribed opposite velocities. Prove sharp
duration and kinetic-action bounds, then vary the speed and acceleration
premises. This returns the programme to mechanical realization of its scales.

**G02** continues this companion estimate by weakening access to the internal
label and testing observability under composition. Uniform lower frame bounds
and uniform upper total susceptibility must be tracked together. For varying
mass, normalize action bounds by $2m$. M03 remains an independently audited
but unfinished spectral draft; this G01 paper does not accept the rest of it.
A09 retains preparation independence and the coherent-action selection task.

The preceding checkerboard commit `f6e6d4f` was verified on origin/main.
Other-session historical/refinement commits `d5070e6` and `5903f67` were
preserved during this work, as were subsequent I003 changes. The concurrent
session had allocated B19, so this audit was renumbered to B20 and its B19
source batch restored exactly. The Makefile check line was included in the
other session's `df50078` checkpoint; this commit supplies its script.
