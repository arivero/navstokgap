# R20: hidden clock offset leaves an exact shell phase interval

Completed 2026-09-11: [proof](../../notes/hidden-clock-ambiguity.md),
[review](../../reviews/hidden-clock-B51.md), C096–C097 and receiver section 25.

R19 was already committed as 03c9123 at session entry. This task advanced R20.
The scaled eight-record map extends smoothly through zero coupling. Solve its
constraint for receiver/probe positions as the clock varies; impose energy
through a second scalar implicit equation. Offset differentiates the free
signal as -AM/v. A fixed early-pulse design has determinant D(r epsilon)^10
and positive trace of -A inverse partial_v A. This selects a shell point with
nonzero transverse energy derivative and both canonical phase derivatives.
At positive small coupling speed compensates energy while offset remains free.

The common-record interval fits inside half-preparation margins. Its endpoints
give risks k_x sigma, k_P sigma and action product k_x k_P sigma squared.
This product depends on clock preparation; a curve supplies no area bound.
Pulse design epsilon is fixed before all coupling/preparation limits.

One sequential worker requested gpt-5.6-luna, low effort, no descendants,
performed B51: one query, one primary Sontag article, four HTML selections.
Effective settings were not independently reported. Coordinator checked metadata,
equations (1)–(4), Definition 1, Proposition 1 and Observability in primary
HTML/MathML. Source coverage and model derivation remain separate; novelty
is unassessed. No numerical/symbolic verification scripts ran.

Next R21: reveal initial clock position, keep clock momentum unknown. Near the
selected shell point, append H_s to the eight-record map in (z,q,v).
The energy-speed Schur complement is nonzero, suggesting a local inverse.
Prove a uniform neighbourhood/stability estimate rather than a pointwise
rank count; state the restriction to a local prepared receiver patch. The
global shell question may have multiple branches.

Validation completed on autonomous restart, 2026-09-11: `make check` passed
1077 local links and source integrity checks; `make papers` built all 14 PDFs;
`git diff --check` passed. The inherited R20/B51 work is checkpointed separately
before beginning R21, preserving its derivation and source-review record.
