# A02 draft checkpoint

The elastic collision bath gives a physical-time action observable
$\mathsf a(t)=2m\int_0^\infty\operatorname{Cov}(V(t+r),V(t))\,dr$
with $\dot{\mathsf a}=\beta(H_*-\mathsf a)$ and
$H_*=(m+M)s^2/\nu$. This advances the physical-time part of the target.
Its plateau retains explicit bath and mass dependence.

Inputs: A01/B09, central target, Principia/action skill and routed regulator
note. Outputs: `papers/collision-action-relaxation.tex`,
`scripts/collision_action_checks.py`, `out/collision-action-checks.json`.
All 15 finite checks pass; the paper awaits B10 prior-art/proof review.

The collision clock and fresh bath are stated stochastic premises. Binary
collisions conserve momentum and energy; a spatial encounter model remains
the next step. Bath velocity scaling preserves the current premises while
shrinking the positive plateau continuously to zero.

Planned B10: one fresh Luna medium worker, at most three primary sources and
eight selected pages. No concurrent coordinator research. Proposed claim IDs
C022 collision plateau, C023 physical-time and window relaxation, C024 scaling
countermodels; acceptance follows coordinator source/proof verification.
