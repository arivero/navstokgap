# R28: calibrated canonical ambiguity

Completed 2026-09-12: [proof](../../notes/calibrated-canonical-ambiguity.md),
[review](../../reviews/calibrated-canonical-B59.md), C112–C113 and receiver
section 33. Live R27/B58 superseded the older R24 restart memory.

The result settles R28's existence branch for a fixed smooth pulse design.
It retains q_1=c nonzero, both exact initial energies E,H_0, all ten final
apparatus records at known T, and the fixed positive preparation box.
R25's compensator centred at eta_c gives an exact common record. Dividing the
calibration and apparatus-energy residuals by lambda gives the smooth limiting
rows -(K/M_1) integral t h x and -Kc integral h xdot. The latter uses clock
reaction, first-order initial kinetic energy and endpoint integration by parts.

A narrow positive first pulse at a small fixed tau leaves a kernel direction
close to Phi_(-tau)(0,0,Y,0), with nonzero initial x and P. The pulse width
is then fixed. Three other pulses preserve R06's signal rank. On the kernel
plane, choose a receiver shell point energy-orthogonal to this direction.
The two divided constraints, receiver energy and a curve parameter have an
invertible derivative. Joint smoothness and a uniform contraction margin
continue a curve on a fixed (lambda,t) rectangle with b/2 box margins.
Its endpoints give epsilon_x epsilon_P >= delta squared |v_x v_P|/4 > 0.
This is a preparation/energy/design-dependent action product, not a projected
area or exact minimax constant. Arbitrary pulse designs are not settled.

Source capsule: inherited B48/B54/B56 contraction and smooth-flow methods ->
regularize the two constrained residuals by coupling -> use the actual shell
rank to continue a canonical curve. Coordinator checked Freire's primary
pp. 1–3. B58's topological result motivates locating the hidden coordinates;
the present proof does not use Borsuk–Ulam. Novelty remains unassessed.

B59 used one sequential gpt-5.6-luna worker at requested low effort, no
descendants, zero queries and zero fresh retrievals; its coverage is inherited
companion evidence. Effective settings were not independently reported. A
follow-up clarified that coverage wording. Coordinator reviewed every displayed
calculation and the worker's regularity/rank/margin obligations in writing.
No numerical or symbolic verification scripts ran.

Next R29: reveal q_2=0. Its divided residual adds a row proportional to
integral t f_2 x. Test local invertibility against the present curve tangent,
then test global common-record pairs. The exact energies and preparation
intersection must remain in force; isolated local solutions do not establish
global recovery.

Validation: `make check` passed 1235 local Markdown links, source companions,
citation keys and source hashes. `make papers` built all fourteen PDFs.
Receiver section 33 was visually checked on final page 17 and programme
updates on page 3; the receiver now fits in 17 pages after mathematical
typesetting cleanup. No build overflow or unresolved references.
`git diff --check` passed. Twelve unchanged-content PDFs were restored after
text comparison. Only document/integrity tooling used Python.
