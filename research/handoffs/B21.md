# B21 handoff: bounded-acceleration librarian audit

- Task: B21 / A07 prior-art audit of bounded-acceleration return and sampled-cut limit.
- Role: librarian; date 2026-09-08.
- Requested model/effort: gpt-5.6-luna, low; no descendant; effective setting
  independently unverified.
- Inputs: `research/CUT_POINT_TARGET.md`, A07 task entry, `AGENTS.md`,
  `agents/PROTOCOL.md`, `skills/principia-action/SKILL.md`.
- Output: `docs/batches/B21/acceleration-source-companion.md`.

## Coverage and findings

Two web searches were used, selecting at most two primary sources and no more
than four pages. Liberzon (2012), chapters 4–5, supplies the general bounded
control framework (metadata-level route only). Acosta–Durán (2004) supplies a
sharp Poincare-inequality reference (metadata/abstract level only), but is not
the interval Lipschitz-variance calculation. No exact prior-art match for the
opposite-endpoint-velocity kinetic-cost formula was found; classify it as an
elementary specialization, not a novel optimal-control theorem.

The companion records the sharp feasibility condition (T\ge2u/a), attaining
minimum (mu^3/(3a)), force form (m^2u^3/(3F_{\max})), closure as (u\to0),
and the polygon error bound (ma^2T|\pi|^2/24). It explicitly distinguishes
kinetic cost from full Lagrangian action and quantum gaps, and records the
physical test: endpoint speed lower bound plus force ceiling supplies scale;
speed ceiling alone allows zero.

## Checks and next action

Checked that both permitted output paths were free before writing. Checked
dimensions: (mu^3/a) and (m^2u^3/F_{\max}) have action units, and the
polygon remainder has action units. No PDF was available for visual equation
verification, so all source readings remain metadata/abstract level. Coordinator
should independently verify the envelope proof and integrate the result into
A07, then update task state.
