# R07: a causal information region

Completed 2026-09-10 from f2ba7fe. Live repository state already contained
R06/B37 and the earlier B36 source work; this session resumed with R07.
Read its state, programme, task and R06 handoff; retained the research skills,
source-idea capsules, no-script rule and sequential agent protocol.

The chosen information restriction is perfect earlier records with no access
to an unresolved bounded force during a fixed delay. C070 gives sharp
coordinate minimax errors; C071 gives their joint reachable lens and canonical
area. The force is a class of admissible external inputs, not assumed stochastic
noise or the fully reconstructed autonomous receiver of R06. The positive
information area is supplied by force budget and delay, with explicit closing
limits. It imposes no lower action on every individual trajectory.

One gpt-5.6-luna worker at requested low effort completed B38: two searches,
one institutional passage and two failed retrievals. Effective model/effort
was not independently verified. Coordinator waited, then checked the source
and written proof and corrected an unverified source title. No descendants,
simultaneous workers or numerical/symbolic scripts.

Outputs: notes/causal-force-information.md; B38 batch and companion;
reviews/causal-information-B38.md; receiver manuscript summary and C070–C071;
state, programme, targets, task queue and idea capsule.

## Validation

`make check` passed: 821 local Markdown links, source companions, citation
keys and all registered source hashes. `git diff --check` passed. Rebuilt
the two affected manuscripts and PDFs (receiver and programme), with three
LaTeX passes each; logs contain no warnings, undefined references or overfull
boxes. Visually inspected the receiver's final page and shortened its closing
cross-reference paragraph to avoid a two-line spill onto a seventh page.
The receiver PDF is six pages. All mathematical checks were written proof
review; Python ran only the permitted repository-document checks.

## Next bounded step: R08

For the same bounded-force class, compose reachable phase-space descriptions
across an inserted cut. An unobserved cut must preserve the old endpoint set
after elimination; an observed cut supplies new information. Test position-only
projection against full $(q,p)$ composition and track canonical area at fixed
physical horizon. This is the direct finite model of the ancient-cut question.
Keep delay refinement separate from mere insertion of an unobserved node.
