# Q11: shared-resource event competition

2026-09-14. Exploratory construction; no accepted-ledger promotion.

Before calculation: test whether changing Q09's independent receivers to a
single shared release can give exclusive events with Q08's fringe weights.
Specify an autonomous jump model, energy budget, readout deadline and delayed
inhibition alternative. Stop at exact event probabilities and identification
of the resource/locality premise; do not treat the jump model as a derivation
from Newtonian mechanics. Q10's separate-channel reliability test does not
settle this architecture.

Source capsule: Q08 supplies complementary work records, Q09 the failure of
independent equal thresholds, Q10 the importance of kinetic control. A single
source query identifies Gillespie's 1977 competing-reaction framework as the
standard probability construction. This is metadata/discovery coverage, not
an exact physical detector or source-proof audit. No worker is dispatched;
independent proof and literature review precede any ledger promotion.

## Result and consequence

[The construction](../../notes/shared-resource-events.md) integrates the
shared-release survival law, obtains fringe-weighted conditional events under
linear hazards, and exposes a finite-time efficiency bound. A distinct delayed
local-inhibition model gives exact double-event probability and a balanced
timing feasibility interval. Stored release energy and rearming are accounted
for separately from signal energy. These are model calculations, not a
microscopic origin of the supplied jump states or universal action.

The next selected test removes shared control and asks for a coincidence
constraint under independent monotone local responses and explicit source
conditioning. After the threshold/reliability/competition sequence, further
detector tuning is parked. No ledger promotion or worker dispatch. Existing
untracked Q02 files remain untouched.

Validation: `make check`, `make papers` and `git diff --check` passed.
A TeX escape error in equation (3) was corrected before the successful build.
The PDF has three pages; the timing/communication page was visually inspected.
There were no unresolved references or overfull boxes in the successful build.
Unchanged existing PDFs were restored after extracted-text equality, preserving
publication bundle correspondence. Markdown, generated LaTeX and compiled PDF
are checkpointed together. No numerical or symbolic verification was run.
