# R13: finite-horizon phase minimax recovery

Completed 2026-09-11 from HEAD dcd9df2. C082–C083 give the exact transient
from a known initial phase state and composition across different constituent
regimes. The worktree was clean on entry; no existing user changes were removed.

## Inputs, outputs and review

Read live STATE, PROGRAMME and TASKS; R12 handoff; R11/R12 notes; I006;
the bibliography/Principia skills, their idea bridge and action/cut routes;
B42/B43 companions. Recovered the autonomous workflow memory skill.
Output: notes/finite-horizon-minimax.md, receiver manuscript §18,
C082–C083, B44 companion/batch/worker handoff, written review and restart state.

One sequential librarian was requested as gpt-5.6-luna at low effort; effective
settings were not independently reported. No descendants. Coordinator waited
for completion and reviewed all outputs before integration. B44 reports two
queries, two sources, four selected passages; the query strings were not saved.
Coordinator corrected Johnson's author attribution and the distinction between
the source's future-supremum risk and fixed-time recovery. The existing source
already supplies zero initial data for its prepared pair. No exact transient
match was obtained in this bounded coverage; novelty is unassessed.

## Result and source-to-model capsule

With tau=sqrt(m epsilon/F), s=T/tau, the exact coordinate radii are
Q=epsilon min(s^2/2,1), P=sqrt(mF epsilon) V(s). V is s up to sqrt(2),
sqrt(2s^2+4)-s up to 4, then 2. Constant acceleration, a negative/positive
bang path, and a delayed prepared pair attain the branches while respecting
the entire record band. One path attains both coordinate support extrema;
this proves the sharp blind-delay extension too.

R12's product and aggregate-image results hold at every T. Position information
is strictly lost under aggregation exactly when constituent force reach and
precision caps straddle their crossing. Equal m,epsilon and forces F,16F at
T=tau give Q_A=3 epsilon/4 versus Q_B=epsilon and P_A=9 sqrt(mF epsilon)
versus P_B=(sqrt(714)-17) sqrt(mF epsilon). Identical-copy products remain
extensive at all horizons. Early H=F^2T^3/(2m); fine precision closes H.

Source → construction → next test: Seeber–Haimovich v2 Prop. 3.1 supplies
the prepared pair, and R12 supplies product-fibre composition. The path-strip
rearrangement resolves finite preparation. R14 now replaces freely aligned
errors by a pointwise quadratic shared apparatus budget for two identical
constituents, then derives the centre risks and aggregate image. This is a
specific test of the product-error premise, not a claim that a universal action
scale or quantum phase parameter has been selected.

## Validation

Written review checked the weighted-acceleration inequality, the full strip,
attainment and junctions, simultaneous support extrema for blind prediction,
aggregate surjectivity, the strict position-loss criterion, the mixed-regime
example and action units/limits. No numerical or symbolic scripts were created
or run. Document builds and repository integrity checks are recorded below.

`make check` passed: 955 local Markdown links, source companions, citation keys
and every registered source hash. `make papers` built all 14 PDFs with no
unresolved references or layout overflow. The receiver PDF has 9 pages; its
new §18 on page 9 was visually inspected. Twelve unaffected PDFs had identical
extracted text and were restored to avoid metadata-only changes. The receiver
and programme PDFs are retained. `git diff --check` passed.
