# R33: correlated calibration response, stage 1

2026-09-12. Stage 1 complete; R33 remains active. Entry HEAD 11abf9b already
completed R32's pulse-column test, so this session advanced its R33 handoff.

## Result and scope

[Derivation](../../notes/correlated-calibration-response.md), C122:
for C-C_*=alpha L e_y, abs(alpha)<=epsilon, the exact reference-record
curve is w_lambda(s)=s e_y-lambda L^{-1}V(s e_y)+O(lambda^2 abs(s)).
Its canonical conditional risk product is O(min(lambda s_0,epsilon)^2),
while its hidden-coordinate extent stays finite at fixed positive tolerance.
V(w)=partial_lambda^2 N(0,w)/2 uses the exact compensated map and moving
coupled reference. Nonzero entries of -Pi L^{-1}D V(0)e_y would give the
matching conditional lower order, but have not yet been established.

Pulses, component units, box and receiver energy ball remain R32's fixed
ones. The scalar correlated error model has three exact complementary
constraints. The estimate concerns a selected compact local fibre; it is
not an upper bound for global minimax risk across other records.

## Source and review

One sequential worker requested Luna low, no descendants; coordinator waited
then reviewed. B65: zero discovery queries, one specified Freire primary route
retrieved by worker and coordinator. The coordinator checked pp. 3–5 and
corrected the worker's p. 4 matrix-inversion anchor to p. 5. Effective model
settings unverified. Inherited B62 supplies compact scalar information radii.
Source methods are standard; exact model matching and novelty unassessed.

Capsule: smooth inverse theorem -> uniform anchored response expansion ->
differentiate the exact moving-reference map twice -> decide canonical
nonvanishing by the projected mixed derivative, rather than a rank count.
Written [review](../../reviews/correlated-response-B65.md) accepts stage 1.

## Next bounded step

Write the first and second mechanical variational equations for
N(lambda,w)=C(eta_lambda(w,a_lambda))-C_*. Include the coupling derivatives
of a_lambda=S^{-1}G_lambda(0,eta_c). Evaluate D V(0)e_y and project through
L^{-1}. If a coefficient vanishes, determine whether this is an exact
symmetry or a higher-order cancellation. Keep the established upper estimate
even if this next calculation remains inconclusive.

## Validation

`make check` passed 1340 local Markdown links, source companions, citation
keys and source hashes. `make papers` built all fourteen PDFs without reported
unresolved references or overfull boxes. The new inverse-expansion passage on
receiver p. 21 was visually reviewed. Twelve unrelated PDF rebuilds were
restored after their extracted text matched HEAD. `git diff --check` passed.
No numerical or symbolic verification scripts were created or run.
