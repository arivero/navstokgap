# A03: refinement consistency checkpoint

The reviewed cut-point calculation separates one consistent Gaussian
experiment from a changing preparation. Exact restriction consistency fixes
$\kappa$; the M05 finite-defect sequence changes retained-node variances.
One inserted node contributes mean kinetic action $\kappa/2$, while ordinary
constant-force chord errors vanish on all shrinking nonuniform meshes.

Accepted C027–C029: [maintained note](../../notes/cut-point-consistency.md),
[generated TeX](../../papers/cut-point-consistency.tex),
[PDF](../../out/papers/cut-point-consistency.pdf) and
[nine exact checks](../../scripts/cut_point_checks.py).
The build regenerates TeX from Markdown using the research-note template.

B12 was one fresh Luna medium librarian, with no concurrent coordinator
research. It used the existing Pitman–Yor and Newton source routes. Coordinator
review reread printed pp. 12–13, visually checked p. 13 and verified the PDF
hash. The zero-variance case was clarified in the review. Source inputs and
elementary consequences remain separate in the ledger. B11's earlier polygon
audit remains pending and was not silently accepted through this task.

The Principia/action skill kept cut insertion distinct from physical-time
relaxation. Source-companion practice retained inherited reading coverage
separately from new coordinator verification. No new source downloads or
dependencies were needed. Run `make papers`, `make check`, and staged
whitespace checks before pushing. The accepted build contains seven PDFs.
Verification passed: all nine new checks and earlier suites, 262 local links,
source/citation/checksum checks, and seven PDF builds with resolved references
and no layout overflow. The new note is three pages; page 2 was visually
inspected and duplicate automatic heading numbering removed in its template.

Next: A05, one explicit physical-cut intervention. Derive its conditional
variance and energy/momentum transfer, test coarse-marginal consistency and
finite speed, then ask what forces a positive universal action parameter.
Coordinate insertion alone permits the deterministic zero family.

Cost: user excludes high/xhigh, permits selective max-effort second opinion,
and prohibits ultra. No max-effort review was needed at this milestone.
