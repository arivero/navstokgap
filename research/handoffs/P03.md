# P03: repository review checkpoint

2026-09-09. Coordinator review of the research programme, proof families,
manuscript structure, sources and tooling. Starting checkpoint: `0632c7d`.

## Outcome

Read the [review](../../reviews/repository-review-2026-09-09.md) and
[I004](../../ideas/I004-bound-action-and-transport.md). The next pair is M07's
known relativistic Kepler threshold and A15's observable for bound mechanics.
The transport models remain valuable preparation laboratories. A14 is a
bounded follow-up, A16 tests dilation premises, and R02 reviews finite propagation.

The I003 draft now uses a consistent Fourier convention, separates conservation
from positivity, retains the exact two-state commutator identity, and replaces
the commuting-variance claim by its ballistic counterexample. Its resolution
model explicitly assumes independent additive errors. The scalar companion
retains its candidate theorem with a repaired fixed-time logarithm argument.
Original user remarks and historical source routes are preserved; prior versions
remain in Git. These are draft repairs, with R02 review still open, rather than
new accepted claims. Accepted C001–C051 remain unchanged.

## Source work and cost

One sequential **Sol, medium** worker performed B27 with fresh context:
three discovery queries, one primary PDF, four selected pages. Coordinator
read the handoff and visually checked all four pages. Boyer's threshold and
circular minimum are established results. Metadata, checksum and coverage are
in B27; no broad historical corpus or full Navier–Stokes proof audit was claimed.
The source's time-parametrisation prefactors are outside the adopted benchmark.

## Infrastructure and restart

`make figures` now refuses the legacy generator because it executes symbolic
verification. Maintained imperative check instructions were retired; existing
scripts, figures and outputs were preserved. `make check` and `make papers`
remain document-only workflows. No numerical/symbolic verification script was
created or run. No advanced tools were installed.

STATE, programme version 17, TASKS, paper index and source-idea capsule route
the next session. Historical milestone wording is labelled as history. The
Millennium definition note now points to the already public OpenAI artifact
and its existing first-reading companion.

## Validation

`make check` passed (646 local Markdown links, source companions, citation keys
and registered source hashes). `make papers` rebuilt all fourteen accepted PDFs
with reference/layout checks. Programme page 2, containing the new priority,
was visually inspected. `git diff --check` passed. The document checker now
guards the disabled legacy figure target as well as the document-only check
recipe. These checks concern documents and workflow; R02's mathematical
acceptance remains a future written-proof and librarian review.
