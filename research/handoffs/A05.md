# A05: finite-speed physical-cut checkpoint

The equal-mass elastic example has an explicit momentum receiver and random
midpoint. Its action coefficient is $mu^2\Delta$, with fixed pair energy
$mu^2$. The general midpoint bound is
$\kappa_{\rm mid}\le m\Delta(u-|v|)^2$. A ballistic position-only
convolution semigroup is deterministic drift; velocity memory supplies the
next refinement construction.

Accepted C030–C032: [note](../../notes/physical-cut-speed.md),
[generated TeX](../../papers/physical-cut-speed.tex),
[PDF](../../out/papers/physical-cut-speed.pdf),
[twelve checks](../../scripts/physical_cut_checks.py).
B13 was one fresh Luna medium worker; the coordinator waited and reviewed
after completion. No max-effort run was needed. Two cached primary sources,
six selected pages: Barkai pp. 1, 3–5; Cinque pp. 1–2. No new downloads.

Coordinator verification recalculated both PDF hashes and visually checked
Barkai p. 4 and Cinque p. 2; Barkai pp. 3–4 were also read in extracted text.
Corrected equation-page anchors and distinguished the equal-mass map from the
source's divided-density formula. The geometric support proof, biased-midpoint
term and narrow convolution hypothesis were checked directly.

The Principia/action skill kept physical execution distinct from coordinate
insertion. The source-download skill maintained explicit cached provenance.
The elastic example changes the receiver preparation when duration changes;
it preserves coarse positions, not the full phase-space preparation.

Next A06: finite-speed $(X,V)$ bridge, conditional midpoint with endpoint atoms,
coarse/fine restriction and action-scale limits. Keep endpoint conditioning
and memory explicit. B11's earlier polygon-source review remains pending.

Reproduce with `make papers` and `make check`; eight PDFs are in the accepted
build. Check staged whitespace and push after verification.

Verification completed 2026-09-08: all twelve new checks and earlier suites
pass; 278 local links, citation keys, companions and tracked source checksums
pass. All eight PDFs build without unresolved references or layout overflow.
The new note is three pages; page 2 was visually checked for the midpoint
and convolution formulas.
