# R12: worst-case composition and record aggregation

Completed 2026-09-11 from repository HEAD 169dfef. C080–C081 give exact product
minimax radii, extensive identical-copy action products, and the exact cost of
retaining only aggregate position records. A08's variance universality does not
transfer to this observable and product information class.

## Inputs, outputs and review

Recovered live STATE, PROGRAMME, TASKS, R11 handoff, I006, A08/R11 notes,
ACTION_FIELD_TARGET and the bibliography/Principia skills. The source capsule
was B42's Seeber–Haimovich prepared pair plus A08's nonnegative additivity proof.
Output: notes/minimax-composition.md; receiver manuscript §17; C080–C081;
B43 source companion/batch; reviews/minimax-composition-B43.md; updated restart
state, programme, tasks and idea bridge. Prior untracked H10 source files,
I007 and static-composition-classics were preserved and excluded from checkpoint.

One sequential librarian requested gpt-5.6-luna, low effort; effective settings
not independently reported. Coordinator waited, then reviewed all outputs and
source passages. No descendants. B43 used one query and two primary sources.
The worker's within-four-passages assertion is not verified: it lists three
reused differentiation passages and three geometry selections. Coordinator
pinned the latter to v2 and recorded exact equation anchors. No extra search
sweep or worker was launched.

Written proof review checked product extrema, canonical masses, arbitrary signs,
known initial data, common preparation time, both directions of aggregate-image
equality, equality conditions, units and the additive-function closure premise.
Continuity and closedness assumptions were made explicit. Source review retains
the distinction between future-supremum differentiation risk and the fixed-T
hidden-pair argument. Literature status is derived consequence/unassessed novelty.

## Result and source-to-model capsule

For product classes retaining all constituent records,
Q_R=sum(m_i Q_i)/M and P_P=sum(P_i). Thus identical-copy H_R=nH_1.
With complete noisy position histories, F_i,epsilon_i positive and
T >= max_i 4 sqrt(m_i epsilon_i/F_i), Q_R=E and
P_P=2 sum sqrt(m_i F_i epsilon_i). Keeping only the centre record has exactly
the single-body image (M,sum F_i,E), giving P=2 sqrt(M E sum F_i).
Strict loss occurs unless F_i/(m_i epsilon_i) is common.

Source → construction → next test: Seeber–Haimovich v2 Prop. 3.1 supplies
the prepared hidden pair; Ruan–Chirikjian v2 §3.1 supplies Minkowski addition
context. Product fibres explain extensive risks. R13 tests finite-horizon
risks before that pair fits, and composition across different transient regimes.
A shared-force/error constraint is a secondary test of product-fibre closure.
The physical record-precision selection and quantum-phase gates remain open.

## Validation

`make papers` built all 14 PDFs. Twelve unaffected PDFs had identical extracted
text and were restored; receiver and programme reading editions are retained.
The new receiver §17 was visually inspected on page 8. Both affected LaTeX
logs have no warnings, undefined references or layout overflow.

The shared-worktree `make check` stopped on the pre-existing untracked
`docs/batches/H10/SchemmelBoltz_MohistCanon_2023.pdf` lacking a registered
checksum. Preserved that parallel source work unchanged. An isolated export
of the staged R12 checkpoint passed `make check`: 911 Markdown links,
source companions, citation keys and all registered source hashes.
`git diff --check` passed. Mathematical validation used written proof and
source review; Python was limited to document tooling and PDF text comparison.
