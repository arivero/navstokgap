# R24: full clock phase recovery and preparation comparison

Completed 2026-09-12: [proof and comparison](../../notes/full-clock-phase-recovery.md),
[review](../../reviews/full-clock-B55.md), C104–C105 and receiver section 29.

The leading ten-record map is (-A_(s,v)z,q,s+theta v,v), theta=T V_*/S_*.
Clock output differences give |dv|<=d and |ds|<=(1+theta)d. Uniform signal
derivatives give the receiver bound and a positive global beta; complete C1
reaction is O(lambda), so the exact inverse margin is beta/2. Compact residual
fitting yields the canonical error-product bound with four record error classes.
Both initial clock coordinates and exact receiver energy are unnecessary.
Known incoming zero probe momenta and joint final access at known T remain.

R19–R24 are consolidated in a preparation/record comparison table. The next
task removes a shared premise instead of adding another clock variable.

B55 used one sequential gpt-5.6-luna worker requested low effort, no descendants,
zero queries and zero source retrieval. Freire pp. 1–3 and Sideris pp. 89–90,
92–93 are inherited B54/B50 coverage, not fresh reading. Coordinator corrected
the inherited page range and fixed/joint limit wording, reviewed all new
equations in writing, and kept novelty unassessed. No mathematical scripts ran.

Next R25: unknown full initial apparatus state a, full final apparatus records.
At zero coupling D_a records is the invertible free shear and D_z records=0.
For small coupling, precondition with that shear inverse and test contraction
in a around the nominal apparatus preparation. Keep z on the exact receiver
shell, quantify an interior b/2 margin, and distinguish preparation-dependent
reconstruction risks from universal positive action. This may consolidate
R17 with the complete-record case; no extra source sweep is needed to select it.

Validation: `make check` passed 1157 local Markdown links, citations, companions
and source hashes. `make papers` built all fourteen PDFs. The receiver has
fifteen pages; section 29 was visually inspected on its last page. Receiver
and programme logs contain no warnings, undefined references or overfull boxes.
`git diff --check` passed. Unchanged-content PDF timestamp churn was excluded.
