# R01 handoff

- Task: bounded independent mathematical audit of the first technical paper.
- Role/model: Codex independent reviewer, inherited agent model without override.
- Date: 2026-09-05.
- Repository base: `2cb1554f71ac5073a113802dd9ccd5daedb5cadf`.
- Reviewed manuscript: `papers/action-gap-foundations.tex`, all 297 lines,
  SHA-256 `b9b9f4a34eb30794eda005ec97ea360caffea667a46f8d223377709fb592c158`.
- Reviewed claim ledger SHA-256:
  `f7769822f77d31ce4524dccb70dd05cd577be7541d11a8cfe1b75f3e9dd5437c`.
- Outputs: `reviews/action-gap-foundations-R01.md` and this handoff only.

## Inputs and coverage

Read `AGENTS.md`, `research/STATE.md`, `research/PROGRAMME.md`,
`research/TASKS.md`, `agents/PROTOCOL.md`, the repository and installed copies of
the `principia-action` skill, its routed
`notes/principia-constant-force-action.md`, `claims/LEDGER.md`, and the entire
technical manuscript. Also inspected `notes/jacobi-kernels-distinguishability.md`
and the existing `reviews/first-milestone.md` to establish scope and avoid
mislabeling an author review as independent.

All three propositions were proof-audited. The unnumbered area/action identities,
Dirichlet Hessian, oscillator mode calculation, normalized free kernel,
distributional first correction, discrimination asymptotic, conditional duration
substitution and finite-speed example were also audited. No uncertain extracted
mathematical symbols or transcription defects arose; inputs were source text.

No external source search/download was attempted. Existing Newton edition labels
were read as context only; this task supplies no fresh historical verification.
A narrow search of the memory registry had no relevant result and contributed
no facts or procedures.

## Findings and proposed integration

No blocking or medium-severity mathematical error was found. The review accepts
the three propositions within their stated models and supports C001–C008 at the
elementary/conditional level described in the ledger. This is not a formal
certificate, external referee report or novel quantum reconstruction.

Two low-severity scope clarifications are documented with concrete diagnostics:

1. After `eq:area`, specify an ordinary endpoint gauge function `G(q,t)`.
   Position matching does not cancel velocity-dependent endpoint terms; an
   explicit higher-derivative counterexample is in the review.
2. `prop:local` proves small action differences in its scalar parameter.
   Require continuity of `q_a` in a chosen topology if citing it as a statement
   about arbitrarily nearby paths. C003 already includes a continuous direction.

The coordinator may apply these clarifications, link this review from the ledger,
and mark R01 done after checking the scope. The review does not authorize
promotion of H001–H003, Q001 or Q002. Historical claims, novelty, singular
central-force dynamics, experimental implementation and field-theory transfer
remain outside scope.

## Validation performed

Read-only checks used `git status --short`, `git rev-parse HEAD`, `nl -ba` for
manuscript anchors and `sha256sum` for the two input snapshots. The worktree was
already dirty; no pre-existing files were edited.

A read-only `python3` heredoc using installed SymPy independently evaluated:

```text
integrate(m*diff(a*t*(T-t),t)**2/2,(t,0,T)) = T**3*a**2*m/6
substitute(a=F/(2*m))                    = F**2*T**3/(24*m)
integrate(m*(T-2*t)**4/(8*c**2),(t,0,T)) = T**5*m/(40*c**2)
trace((rho0-rhophi)**2)-2*sin(phi/2)**2   = 0
2*acos(sqrt(4*(3/4)*(1/4)))              = pi/3
```

Here `rho0=[[1,1],[1,1]]/2` and
`rhophi=[[1,exp(-i*phi)],[exp(i*phi),1]]/2`. The command exited successfully.
No build or repository-wide test execution was delegated to this reviewer; the
coordinator is performing those independently. No files were committed.

Suggested next mathematical task: M03, keeping oscillator Hessian positivity,
quantum Hamiltonian spacing and volume-dependent free-particle gaps distinct.
