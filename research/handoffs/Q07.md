# Q07: where spin-action integrality enters

2026-09-14. Exploratory premise test; no ledger promotion.

Before calculation: resolve whether global classical spin mechanics repairs
Q06's free common normalization. Construct north/south local actions on S2;
compare consistency of their variational equations with consistency of a
supplied phase exp(iS/K). Stop at the exact overlap condition and test whether
it fixes K, or a minimum local action. Q06 concerns reciprocal forces, not
this global obstruction, so its result does not settle the question.

Source capsule: Q06's closing proposal and Q01's retained distinction between
classical dynamics and quantum structure. One bounded source query locates
prequantization literature; the primary Poisson-sigma-model paper
math/0507223 explicitly derives integrality from path-integral requirements.
Its abstract is checked, not its full proof. The sphere calculation here is
written explicitly; independent review is required before ledger promotion.

## Result and strategic consequence

[The note](../../notes/spin-action-patching.md) constructs the north/south
potentials, their endpoint action difference and the single-valued transition
phase. The phase requires 2s/K integral; classical variational equations
require no integrality. Common rescaling survives, cap-loop actions shrink,
and two irrational-ratio spin spheres admit a smooth classical interaction
but no common-K prequantization. The latter is a direct application of the
displayed patch conditions, not a quantum reconstruction theorem.

No new claim is promoted, no worker dispatched, and no full-source audit
claimed. Next test a mechanical interference readout rather than another
topology or reciprocal-network variant. This addresses the physical phase
premise exposed here; the gap track still lacks independently identified
physical dynamics. Existing untracked Q02 files remain untouched.

Validation: `make check`, `make papers` and `git diff --check` passed.
The two-page PDF was rebuilt after the final source-metadata edit; its
countertest/source page was visually inspected. The build found no unresolved
references or overfull boxes. Other PDFs were restored after extracted-text
equality, preserving existing publication bundles. Markdown, generated LaTeX
and compiled PDF are included in the checkpoint. No numerical or symbolic
verification scripts were created or executed.
