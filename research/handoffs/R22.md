# R22: global clock-speed ambiguity survives positive coupling

Completed 2026-09-11: [proof](../../notes/global-clock-speed-ambiguity.md),
[review](../../reviews/global-clock-B53.md), C100–C101 and receiver section 27.
R21 and its formatting correction were already committed at ad60bee on entry.

The early-pulse limit of -A inverse partial_v A acts on the first four output
derivatives with weights (1,2,3,4)/v. For Q=0 its energy form is
[g(9x squared-14xy+5y squared)+2P squared/mu]/v. A normalized positive-x,
positive-P shell path joins opposite signs. Fix design, then speed separation:
the exact zero-coupling energy mismatch changes sign, with both canonical
separations positive. Uniform implicit continuation of the eight-record map
preserves endpoint signs for small positive coupling. Intermediate value
then gives an exact interacting equal-energy pair inside preparation margins.

The risks obey epsilon_x epsilon_P >= c_x c_P d_v squared/16, with action
units. The full shell and speed preparation supply the ambiguity; R21's
local patch theorem remains valid. No area or universal-scale claim is made.

B53: one sequential gpt-5.6-luna worker requested low effort, no descendants,
one query, one primary source, three HTML passage groups. Effective settings
were not independently reported. Coordinator corrected the false Raue/2010
metadata to Quaiser, Dittrich, Schaper and Mönnigmann, BMC Systems Biology
5:30 (2011), DOI 10.1186/1752-0509-5-30, checked primary metadata and Methods
definitions, and separated local/global from structural/at-a-point scope.
Numerical application examples remain worker-only coverage. Novelty unassessed.

Next R23: reveal final clock momentum, persistent after all pulses, while
initial speed remains unknown. The leading nine-record map is
(-A_v z,q,M_c v). Seek a uniform global inverse on a convex receiver domain
times fixed apparatus boxes, perhaps by using (-A_v z,q,v) as coordinates
and controlling the remainder there. Include record precision and known
offset; do not infer global injection from pointwise rank alone.

Validation: `make check` passed 1117 local Markdown links, citation keys,
companions and source hashes. `make papers` built all fourteen PDFs; receiver
length is fourteen pages and its final page was visually inspected. Receiver
and programme logs have no warning, undefined-reference or overfull-box entries.
`git diff --check` passed. Unchanged-content PDF timestamp churn was excluded.
No numerical or symbolic verification scripts ran.
