# B39 handoff — bounded librarian audit

Task: R08 audit of C072 exact sheared reachable-set composition and rectangle overapproximation, and C073 exact position fibre, terminal area, and area closure.

Requested worker: gpt-5.6-luna, low; effective settings were not independently reported. Date: 2026-09-10. Inputs: `notes/reachable-cut-composition.md`, R07/B38 source companion, first R08 card in `references/IDEA_BRIDGES.md`, `research/CUT_POINT_TARGET.md`, Principia and bibliography skill routes. Outputs: `references/batches/B39.md` and `docs/batches/B39/cut-composition-source-companion.md`.

## Source coverage

Two web searches were used, with a three-passage/page cap. Liberzon §4.4.1 (institutional HTML, lines 4–35) was read at passage level: normalized bounded double integrator, state form, bang–bang controls and one-switch phase-plane curves. Bird et al., arXiv:2106.14831, abstract and HTML §4.1 (lines 3–5, 209–249) were read at passage level: exact forward reachable-set representation for an MLD class and closure under linear mappings/Minkowski sums. The third route found no readable primary segment-area theorem; search results were not used as evidence. No PDF was downloaded.

## Findings

Liberzon is a direct normalized model match, but does not state the fixed-time continuous-time reachable lens or its area. Bird et al. provide adjacent set-propagation support, but their discrete/hybrid assumptions prevent treating them as a theorem for R08. C072 is therefore a derived specialization under arbitrary measurable `|f|≤F` inputs with no cross-time coupling: split at `a`, shear the first segment, and concatenate any two admissible segment inputs. The marginal rectangle strictly enlarges the propagated set because it admits cut position–momentum pairs that no single past force realizes.

C073 has no direct source match. Its fibre, conditional terminal set, and area follow from the R08 lens and determinant-one shear. The observation is ideal and non-disturbing by model stipulation. Area closure as `b→0` does not imply momentum recovery; the central position fibre retains nonzero momentum width. Canonical area means ordinary `dq dp`, with action units `ML²/T` and no `2π` normalization.

## Limits and proof concerns

The audit does not establish delayed-record causality, observation apparatus, preparation independence, a universal action constant, or a positive lower bound as `F` or the residual horizon shrinks. Do not claim exhaustive prior art. Keep exact reachability, rectangle outer approximation, and information conditioning distinct.

## Source-to-model suggestion

For R09, apply the exact set-propagation viewpoint to an interval-valued position record of half-width `ε`; compare the compatible phase-state propagation with a rectangle/zonoid outer approximation and track the discarded correlation explicitly.

Checks: inspected only the three assigned output paths after writing; no shared state, ledger, manuscript, or numerical/symbolic verification files were edited.
