# R32 stage 1 proof review

Accepted C120 as a conditional, local-fibre derivation. The pulse-specific
canonical nonvanishing test remains open.

1. The reference is the exact coupled record through (0,eta_c). Subtracting
   D_lambda(0,a_lambda) makes T_lambda(0)=0 and T_0=Lw exactly.
2. R30 supplies a smooth invertible derivative at zero uniformly near zero
   coupling. A ball strictly inside the receiver energy ball and a uniform
   contraction image radius justify a fixed interval in s. The apparatus
   compensator remains in the unchanged physical box after reducing coupling.
3. T_lambda(w)=s e_j enforces the three exact constraints and changes only
   the fourth by lambda s, including all nonlinear reaction. Thus the allowed
   interval is min(s_0,epsilon/lambda).
4. Differentiation gives J_lambda^{-1}e_j. Both canonical components must
   be tested separately. Uniform continuity then fixes derivative signs on
   the small rectangle; integration between endpoints and the triangle
   inequality give the factor 1/4 in the risk product.
5. R31's upper bound applies with three zero tolerances. The matching order
   is conditional on both components being nonzero; the local construction
   asserts no exact no-record saturation value.
6. A compact smooth curve has zero planar area under canonical projection:
   a Lipschitz parametrization can be covered by O(1/r) squares of side r,
   whose total area tends to zero. Positive coordinate diameters remain
   possible. This concerns the compatible curve, not its convex hull.

No numerical or symbolic verification scripts were used. B63 records bounded
method coverage and the coordinator's primary passage recheck.
