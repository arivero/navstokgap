# R14/B45 written proof and source review

C084–C085 are accepted as derived consequences, 2026-09-11. The domain is
identical positive masses and force ceilings, known initial phases, arbitrary
bounded forces, complete records, and the specified pointwise convex norm
budget. H multiplies separate canonical coordinate minimax risks.

## Mathematical review

For the quadratic pair, averaging bounds the record error by epsilon/sqrt(2).
Equal errors and forces lift every aggregate experiment. For central fibres,
equal displacements lift every scalar path in that reduced band, including
R13's full-time extremizers. Known inertial offsets may differ between
constituents; subtracting them before the lift preserves each initial phase.
The half-difference argument needs convex symmetry, not Cartesian errors.
Compactness follows from the product weak-star compact force balls and the
closed pointwise path-norm constraints. Thus the scalar extrema are attained.

The centre canonical momentum is the sum, hence mass 2m rather than m.
R13 with (2m,2F,E) yields the stated P; its acceleration ratio stays F/m.
Simultaneous positive coordinate support extrema permit the blind-delay
extension. The early product and 2^(1/4) late factor follow with action units.
At zero horizon both errors vanish; at fixed positive horizon fine precision
closes the product.

For n copies, Holder's inequality gives sum |e_i|<=n^(1-1/r)||e||_r;
the endpoints r=1 and infinity follow directly. Equal components attain the
average bound, and the synchronous lift respects every force ceiling and
every time-indexed error constraint. The canonical momentum contributes n,
while E_n^(3/2) contributes n^(-3/(2r)). The saturation horizon is reduced
by n^(-1/(2r)); constant copy-count product at r=3/2 refers to saturation,
not every finite horizon. Fixed global budget and independent block budgets
are different preparation classes. This distinction is retained in R15.

## Source review

B45 used one sequential Luna-low worker. Coordinator checked the named
primary publisher abstract and versioned arXiv Introduction, corrected source
anchors and recorded the limited coverage. The references supply context;
they establish neither an exact mechanical exponent match nor novelty.
No numerical or symbolic verification scripts were used.
