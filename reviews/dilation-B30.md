# A16/B30 coordinator review

Accept C056–C057 as conditional written derivations. Differentiate the scaled
trajectory to obtain the transformed Hamiltonian equations; the chain-rule
factor is 1/a in the force and absent in velocity. Conjugacy of flows gives
stationary pushforward laws. The determinant of diag(a,1) is a, so its covariance
determinant factor is a² and its positive square-root factor is a. Changing
time variables in the transport observable gives a h(Delta/a), not a h(Delta).
These checks require no numerical or symbolic scripts.

The proof of zero infimum only requires a sequence of admissible a tending to
zero. The single nontrivial contraction argument for a common K requires both
models to belong to the class. Energy and mass can stay fixed while the
coupling changes. Kepler, softened and harmonic substitutions were checked
directly; force and acceleration scale inversely. A particle speed ceiling
says nothing here about propagation of the external interaction.

One sequential Luna-low audit supplied B30; the coordinator waited, then
verified primary-source metadata and the named conformal-scaling statement.
The two-source HTML coverage gives related prior art, without an exact match
or a novelty claim. The geometric source concerns scaling symmetries; A16
allows changed Hamiltonians. No extracted source formula is used as a premise.
A17 now tests the proposed force-bound escape at fixed potential.
