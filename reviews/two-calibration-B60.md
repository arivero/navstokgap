# R29 review: two regular roots with one global record

C114–C115 are accepted as checked derivations in
[R29](../notes/two-calibration-branches.md).

1. The second pointer's exact drift identity gives W_0=-(K/M_2)A_2.
   Its numerator vanishes at zero coupling for every receiver state, so
   integration of its coupling derivative gives joint smooth divisibility.
2. In the evaluation limit the first rows fix x(tau)=P(tau)=0. The third
   row has Y coefficient g tau squared/(2mu)+O(tau fourth), giving
   Y=-Q tau/(3nu)+O(tau cubed). Backward evolution yields the two coefficients
   in (2), including the negative x and positive P signs. Rank three and
   both nonzero components persist at fixed positive smooth pulse width.
3. The one-dimensional kernel intersects the positive energy shell at two
   opposite points. At each, dH_s is nonzero on that kernel; thus the full
   four-row residual derivative is invertible. The nominal root stays exact
   at positive coupling and the opposite root continues by a uniform local
   contraction. The original two energies and both calibrations hold exactly.
4. The apparatus compensator is available on both receiver neighbourhoods
   with common O(lambda) displacement bounds; fixed preparation margins
   and the linear cutoff regions persist after reducing coupling.
5. Eliminating the invertible apparatus-record derivative in the fourteen
   by fourteen augmented map leaves the four residual rows, three scaled
   by lambda. At fixed positive lambda this proves a local inverse at each
   branch. Continuity plus segment integration supplies local stability;
   no uniform unscaled inverse as lambda tends to zero is claimed.
6. The two common-record states approach opposite shell points with nonzero
   x and P. Separate endpoint inequalities give the action-unit risk bound.
   Local isolation is compatible with this global ambiguity.

The model uses a selected pulse design, fixed calibrated nonzero c and a
positive preparation box. Its lower scale remains energy/design-dependent.
[B60](../references/batches/B60.md) supplies inherited-method coverage only;
exact prior-art matches and novelty are unassessed. No mathematical scripts ran.
