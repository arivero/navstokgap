# P04 routing review

This is a read-only routing simulation. It does not perform research, run
verification scripts, update STATE/TASKS, or dispatch a worker.

## 1. Generic autonomous request

**Selected next action:** Q01, the first bounded Hardy/CDP premise-to-use
dependency audit, because it is first in the ordered list in `research/STATE.md`.
The request does not select R33 or another receiver continuation.

**First context to load:** `research/STATE.md`, `research/PROGRAMME.md`, the
Q01 entry in `research/TASKS.md`, `research/STRATEGY.md`, then the Q01 bridge
and B01 source companions routed by the bibliography skill. Load only the
passages needed to state the operational class, exclusion premise, classical
countermodel, and action-unit gap.

**Stopping point:** a reviewable bounded premise-to-use map with exact source
coverage and the remaining action-unit dependency. Do not expand into full
reconstruction-proof audits. After substantive work, the coordinator would
reconcile the handoff and STATE; that state mutation is excluded here.

**User input required:** no routine approval. The explicit autonomous request
authorizes the bounded selection under STATE. Additional source access or a
materially different task would require direction.

## 2. Explicit request to finish R33's curvature calculation

**Selected next action:** resume R33's retained variational capsule and address
the named nonvanishing-curvature question, since an explicit user choice takes
precedence over the generic STATE queue.

**First context to load:** the R33 handoff, its accepted stages C122–C123 and
the task-relevant notes/sources it names; also apply STRATEGY's pre-calculation
record of target, changed decision, deliverable/stop, and why the accepted
stages do not settle curvature.

**Stopping point:** the requested curvature result or a written obstruction
identifying the precise missing premise, with proof/source review. Stop at the
named calculation; do not select a sharper coefficient or unrelated branch.

**User input required:** no. The explicit request is sufficient. A newly
needed external source, expanded scope, or prohibited computational check would
need direction.

## 3. Two same-family steps leave the same premise; sharper pulse available

**Selected next action:** consolidate the two results and park the sharper pulse
coefficient. STRATEGY says a new pulse/coefficient variant is not a successor
when both outcomes leave the same missing physical premise; compare the other
main track and retain an exact return criterion.

**First context to load:** the two completed handoffs and their claim/source
links, then `research/STRATEGY.md`, current STATE, and the consolidation note.
No new pulse calculation is justified merely by availability.

**Stopping point:** a concise consistency review stating the unchanged premise,
strategic consequence, parked question, and return criterion. No new claim or
source sweep.

**User input required:** no routine approval; coordinator routing is autonomous.

## 4. Q01 source route blocked at exhausted budget

**Selected next action:** close the source task at its stated budget, record the
coverage and inaccessible passage, and preserve the remaining dependency. A
different available main-track task may proceed only through normal STATE
selection.

**First context to load:** the Q01 worker envelope, B01/source companion,
access-failure record, and `agents/PROTOCOL.md` budget and handoff rules.

**Stopping point:** an access-limited handoff with exact coverage, failed route,
remaining dependency, and no expanded reading campaign.

**User input required:** only if completion requires a new route, larger budget,
paid service, or external correspondence. Otherwise the coordinator can record
the bounded failure and route the next STATE task.

## Scheduling contradiction to flag

There is no contradiction in the live priority rule: STATE is expressly the
single ordered list, while TASKS says its board is not ordered by dispatch
priority and that “ready” means eligible, not next. However, TASKS still
contains historical dependency/suggestion text such as “after A08 priority,”
and old handoffs may contain “next task” prose. Those statements can conflict
with the current STATE ordering if read as schedules; they must be treated as
context only. The coordinator should preserve this distinction when updating
the board.

## Coordinator resolution

The four task choices above are accepted. The worker's statements that a new
external source or source route automatically requires user direction are not
accepted. Public lawful alternatives inside the existing scope and budget are
autonomous. An exhausted worker budget returns the decision to the coordinator;
it does not by itself require user permission. Paid services, external
correspondence and a new computational-verification workflow retain their
separate authorization requirements. STRATEGY now states this explicitly.
The old B11 "after A08 priority" phrase was removed from the live board.

Review settings: one sequential Luna-low worker, no descendants or web; effective
settings unverified. The coordinator waited for completion and reviewed the
610-word report. This simulation checks selection reasoning; it is not evidence
that future research sessions cannot drift.
