# R33 stage 2 written review

C123 accepts the signed-parameter cancellation, quadratic quadratures and
conditional upper bound. Curvature nonvanishing remains open.

1. Inside R30's linear-cutoff neighbourhood, receiver reversal together with
   coupling reversal preserves the apparatus equations and reverses the
   receiver equations. The reference has zero initial receiver, so its record
   is even in coupling. Uniqueness of the smooth apparatus chart transfers
   the symmetry to N. Signed coupling is only a proof extension.
2. N(-lambda,-w)=N(lambda,w) implies V even and D V(0)=0. Differentiating
   T(-lambda,-w)=-T(lambda,w) at zero shows J even. Neither statement forces
   canonical constancy away from the reference.
3. The first reference apparatus variation is zero. Differentiating the
   backward integrals (10) reproduces the negative probe/clock forces and
   zero terminal data. Their energy row v_0 ell(0) reproduces -Kc B_1.
4. The first receiver variation is independent of w, since the zeroth-order
   probe positions are c,0,0,0. The explicit reference terminal coefficients
   after (11) give a_2; a_lambda is a final-record coordinate, while eta_c is
   the fixed reference preparation. Subtracting this reference cancels all
   terms involving x^(1) from the second apparatus differences. The remaining
   terms are exactly f'_j h x_w, c f''_1 h x_w and sum f'_j x_w u_j.
5. Backward integration gives (13). Kinetic energy contributes v_0 times the
   second clock momentum plus squares of all first momenta, with denominators
   twice their masses. Every V position component has length units and the
   fourth has energy units. Each remaining product is quadratic in w.
6. The anchored inverse remainder stays O(lambda squared abs(s)). Bounding
   coordinates relative to zero proves (15). Centre-to-endpoint separation,
   not opposite endpoints, proves (16) under its explicit remainder condition.
   Both beta entries are hypotheses of that lower bound, not accepted facts.
7. Teschl printed pp. 46–50 supply the smooth-flow/variational method. The
   coordinator checked the extracted passages and formula images on pp. 48
   and 50. The worker's secondary source was not used for acceptance.

All derivations were reviewed in writing. No numerical or symbolic verification
script was created or executed. Full-class minimax risk and universal action
selection are outside this conditional result.
