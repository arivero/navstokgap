# R32 stage 2: canonical inverse columns accepted

C121 is accepted by written derivation and coordinator review, 2026-09-12.
It proves existence of a fixed admissible early-pulse subfamily with all
four single-calibration directions moving both x and P. C120 then gives
matched canonical risk-product order for each constraint separately.

The coordinator checked elimination of y,Q to the fourth-order output
ODE, the invertible initial-jet chart, all four cardinal polynomials by
substitution and differentiation, row factors including the tau in the
energy row, and fixed-unit conversion. The scaled ODE has bounded
coefficients on [0,3], so variation of constants gives an O(tau squared)
matrix error from the cubic equation. No numerical/symbolic scripts were used.

The B64 Luna-low worker confirmed the algebra and requested an explicit
finite-width operator estimate. Section 4 now bounds each positive normalized
pulse average by its support radius times the supremum derivative of its
row function. The inverse identity then bounds the inverse error, preserving
both first-row and second-row entry margins. This resolves that concern.
The fourth signal pulse is selected by analytic observability; all widths,
endpoint margins and the R30 box are fixed before varying coupling or tolerance.

The coordinator reopened NIST DLMF §3.3(i), equations 3.3.1–3.3.3 and
§3.3(iv)'s confluence passage following 3.3.38. These support standard
interpolation methods. The cubic double-root proof is given independently;
no external formula transcription is needed to verify the cardinal table.
The [source companion](../docs/batches/B64/pulse-column-source-companion.md)
separates this method match from the mechanical conclusion. Novelty remains
unassessed. Requested worker settings: Luna low; effective settings not
independently verified. One worker, no descendants.

The early-pulse selection is an explicit additional design premise. Neither
rank alone nor a zero leading canonical entry decides the general fixed-design
case. The scale is still set by calibration precision, apparatus preparation
and pulse response; the compatible curve has zero planar area. R33 retains
the selected design and tests a correlated direction with cancelling leading
canonical entries.
